# 游戏机制待确认：下一次对齐清单

更新：2026-09-18。依据上游任务书 / 接口 v1.0、用户实战反馈，以及[本轮连续三夜实验](自对弈迭代结果.md)。

## 已确认，不再重复询问

- 用户确认 `CoreGeek/main3.py` 结构的参赛包已在内网平台跑通。
- 夜晚机器人分别进攻双方。接口中 `robot.targetTeam` 指定被攻击的阵营，应该与 `teamOur.type` 匹配选敌；不能把所有可见机器人都当成打我方的目标。
- 用户反馈上一版未活过第二晚。目前没有完整连续状态，尚不能确定是基地先毁、炮手先死还是火力不足。
- 已有另一程序的 243 回合动作日志；可确认该次使用 Python 3.11、端口 6666，首夜火箭指令间隔四回合。单靠动作日志不能还原实际命中和扣血。

## 最先核对：墙能否挡住机器人攻击

**机器人距基地 3 格、中间有墙时，会直接打基地，还是必须先打墙？** 路旁炮手或墙在射程内，但没有挡路时，会停下攻击还是继续朝基地移动？移动与攻击是否互斥？

- 本地第一轮按“射程内有单位就停下攻击”模拟，墙很容易吸收攻击，旧程序也能过三晚，未重现实战失败。
- 第二轮增加“继续推进、能打基地时优先基地且不检查墙遮挡”的压力假设，所有方案都无法稳定撑过第二晚。
- 两种结果差别很大，这项比继续增加策略参数更值得先核对。
- **答复 / 对应画面或日志：**

## 再给一小段第二晚连续状态

不需要一次整理整局。优先要第二晚开始及其后 2～3 回合；如果方便，再补基地即将被毁时的连续 2～3 回合。按现有 70 回合白天、60 回合夜晚规则，第二晚从第 201 回合开始。

每回合最好包含：

1. 输入：`roundNo`、`teamOur.type`、基地 / 炮台 / 炮手的位置和血量，炮台等级、`attackRange`、冷却状态，以及机器人位置、血量、类型和 `targetTeam`。
2. 我方动作，以及下一回合输入里的 `lastRoundRoleActionResults` / `errors`。
3. 若只有普通日志，先给这段已有日志即可；缺少的输入字段再按实际需要补。

这能区分：来袭机器人识别错误、炮手未就位、攻击非法、射程判断错误、火力不足或基地隔墙受击。可用现有 `COMPETITION_TRACE` 记录完整请求 / 响应；记录方法见仓库首页维护部分。

**日志 / 回放位置：**

## 随后按需要核对

| 事项 | 最小问题 | 当前本地假设及影响 |
| --- | --- | --- |
| 前两晚波次 | 各类机器人的数量、出生区域分别是什么？能否固定随机种子和换边复用？ | 波次数量及中部出生区都是人为设定，不能用于预测正式存活率。 |
| 碰撞与寻路 | 能否进入同回合其他单位腾出的格子？撞车后会换路吗？ | 起始占用格保持阻挡，同格争夺取消，下一回合重新选择；拥堵程度会影响范围武器收益。 |
| 攻击与死亡顺序 | 本回合受到致命伤的机器人，回合末之前仍能攻击和移动吗？火箭按移动前位置命中吗？ | 先按旧位置计算命中，致命伤机器人仍行动，最后统一扣血。 |
| 火箭合法动作 | 第 10 回合发射后，第 14 回合是否可再发？能否选空格和重复落点？冷却字段如何变化？ | 间隔四回合，允许重复及空格落点。实战 Bot 读取请求中的冷却值。 |
| 弹道与伤害对象 | 加特林 / 电磁弹道被去对方的机器人挡住时怎么结算？火箭有友伤吗？同时击杀如何计分？ | 本地直射会先命中前方机器人，火箭伤及范围内所有机器人；不模拟己方 / 玩家单位的武器伤害。选敌修正无法保证没有附带伤害。 |
| 输入与建筑范围 | 实战 `attackRange` 是否权威？能否给一份现行请求和建造失败反馈？ | Bot 优先读请求射程；模拟器按正文射程和 Demo 建造环计算，可能与引擎不同。 |
| 跨天损伤与升级 | 基地 / 建筑能否修复？升级恢复多少血、需要哪些资源？ | 当前损伤跨天保留，建筑不自动回血；没有交易、升级和任务收入。下一步经济优化需要这部分。 |
| 目标阵营变化 | 机器人会因阻挡或诱导换目标阵营吗？去对方的机器人会不会攻击我方挡路单位？ | 目标阵营固定，机器人仅攻击其目标阵营单位；没有模拟跨阵营挡路攻击。 |

## 可直接转发的短版

> 程序已跑通，也已按 `targetTeam` 修正只选择攻击我方的机器人。当前最想确认：机器人射程内有墙 / 炮手时，是否会停下攻击；距基地 3 格但中间有墙时，是否能直接打基地？另外想要第二晚开始和基地被毁前各 2～3 回合的连续输入、我方动作、下回合执行结果，用来核对真实受伤原因。前两晚波次数量、出生区域，以及建筑修复 / 升级规则可随后补充。拿不到的内容写“暂未知”即可。

## 收到信息后的下一步

先用连续状态校准一处关键机制，再在新的种子上复核混合炮台、火箭比例和炮手站位。只有生存指标改善才替换默认策略；本轮两次复核未通过的候选均保留为实验结果，没有部署。暂不要求新闻、宝藏和全部任务资料。
